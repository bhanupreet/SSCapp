<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssc.sscapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CatalogueActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCompActivity"
            android:parentActivityName=".CatalogueActivity"/>
        <activity
            android:name=".CompanyActivity"
            android:parentActivityName=".CatalogueActivity"
            android:label="@string/title_activity_company"
            android:theme="@style/AppTheme" />
        <activity android:name=".AddPartNo"
            android:parentActivityName=".CompanyActivity"></activity>
    </application>

</manifest>